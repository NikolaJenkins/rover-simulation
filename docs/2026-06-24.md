# 2026-06-24

## Goal

Learn about /odom topic and write node to tell robot to drive two meters

# Issues

The robot's initial point was updating with every method call instead of when the file was initially run, so the robot didn't actually stop.

There was an old robot in the background that was messing with the /odom topic subscription.

# Solution

Added check so the robot's initial point would be changed once at beginning.

Manually shut down all worlds and started new terminal to refresh world.

# Lessons Learned

Learned about /odom topic and used closed loop instructions for robot navigation.
